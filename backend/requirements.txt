# --- Web layer -------------------------------------------------------------
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
flask-jwt-extended==4.6.0
flask-sqlalchemy==3.1.1
python-dotenv==1.0.0
gunicorn==21.2.0
eventlet==0.33.3
bcrypt==4.1.2

# --- Database --------------------------------------------------------------
# MySQL (Laragon / phpMyAdmin) is the primary database.
PyMySQL==1.1.1
cryptography>=41.0.0          # required by PyMySQL for caching_sha2_password
# Kept so an existing PostgreSQL deployment still works via DATABASE_URL.
psycopg2-binary==2.9.9

# --- Computer vision / pose -----------------------------------------------
opencv-python==4.8.1.78
mediapipe==0.10.8
Pillow==10.1.0

# --- Numerics / audio ------------------------------------------------------
numpy==1.26.2
scipy==1.11.4
librosa==0.10.1
soundfile==0.12.1

# --- Machine learning ------------------------------------------------------
tensorflow==2.15.0
scikit-learn>=1.3.0           # KMeans/StandardScaler for gerakan clustering
matplotlib>=3.7.0             # training report plots
